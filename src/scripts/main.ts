/* Entry point: imports the keyboard layer, permalinks, and status-panel ticker.
 * Ordering matters slightly — keyboard.ts sets data-js="on" first.
 */

import './keyboard';
import './permalinks';
import './status-panel';
import './hovercard';
