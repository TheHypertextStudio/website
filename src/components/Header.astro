<header class="site-header">
  <a
    class="site-wordmark interaction-target"
    href="/"
    title="Hypertext Studio — home"
    rel="home"
    translate="no"
  >
    <span class="interaction-label" aria-hidden="true">&lt;hypertext-studio&gt;</span>
    <span class="sr-only">Hypertext Studio home</span>
  </a>
  <nav aria-label="Primary">
    <a class="interaction-target" href="/#products"
      ><span class="interaction-label">Products</span></a
    >
    <a class="interaction-target" href="/about"><span class="interaction-label">About</span></a>
    <a class="interaction-target" href="/contact"><span class="interaction-label">Support</span></a>
  </nav>
</header>

<style>
  .site-header {
    max-inline-size: var(--layout-max);
    min-inline-size: 0;
    margin-inline: auto;
    padding: 1.25rem var(--layout-margin);
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    border-block-end: 1px solid var(--color-rule);
  }

  .site-wordmark {
    font-family: var(--font-mono);
    font-size: 0.9375rem;
    letter-spacing: -0.03em;
    color: var(--color-text);
    text-decoration: none;
    white-space: nowrap;
  }

  .site-wordmark:focus-visible {
    color: var(--color-text);
    -webkit-text-fill-color: var(--color-text);
  }

  .site-wordmark .interaction-label {
    text-decoration: none;
  }

  nav {
    display: flex;
    gap: clamp(1rem, 3vw, 2rem);
    font-size: 0.9375rem;
  }

  nav a {
    color: var(--color-text);
    text-decoration: none;
  }

  nav a:hover {
    text-decoration: none;
  }

  nav a:focus-visible {
    color: var(--color-text);
    -webkit-text-fill-color: var(--color-text);
    text-decoration: none;
  }

  nav .interaction-label {
    text-underline-offset: 0.25em;
  }

  @media (max-width: 32rem) {
    .site-header {
      align-items: center;
      gap: 0.5rem;
      padding-block: 0.625rem;
    }

    .site-wordmark {
      display: inline-flex;
      align-items: center;
      min-block-size: 2.75rem;
      font-size: clamp(0.75rem, 3.333vw, 0.8125rem);
    }

    nav {
      min-inline-size: 0;
      flex-direction: row;
      align-items: center;
      gap: 0.5rem;
      font-size: clamp(0.75rem, 3.333vw, 0.8125rem);
    }

    nav a {
      display: inline-flex;
      align-items: center;
      min-block-size: 2.75rem;
    }
  }

  @media (max-width: 21rem) {
    nav {
      gap: 0.25rem;
    }
  }
</style>
